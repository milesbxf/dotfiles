setw -g mode-keys vi

unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

bind r source-file ~/.tmux.conf

bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

set -g mouse on

# reduce ESC delay for neovim
set -sg escape-time 0

# window movement

unbind p
unbind n
unbind [
bind [ previous-window
bind ] next-window


# more vi-like copy
bind Escape copy-mode
bind p paste-buffer
#bind-key -t vi-copy 'v' begin-selection
#bind-key -t vi-copy 'y' copy-pipe 'xclip -in -selection clipboard'

# theming
set -g default-terminal "xterm-256color"

setw -g clock-mode-colour colour109
setw -g mode-attr bold
setw -g mode-fg colour196
setw -g mode-bg colour238

set -g pane-border-fg colour250
set -g pane-active-border-fg colour237

# status bar



set -g status 'on'
set -g status-position top
set -g status-bg colour237
set -g status-fg colour223
set -g status-attr 'none'
set -g status-justify 'left'

set -g status-left-attr 'none'
set -g status-left "#{prefix_highlight}#[fg=colour248, bg=colour241] #h #[fg=colour241, bg=colour237, nobold, noitalics, nounderscore]"
set -g status-left-length '60'

set -g @online_icon '#[fg=colour66]#[fg=colour239, bg=colour66]    #[bg=colour66]'
set -g @offline_icon "#[fg=colour167]#[fg=colour239, bg=colour167]    #[bg=colour167]"

set -g @batt_icon_status_charged '🔋'
set -g @batt_icon_status_charging '⚡'
set -g @batt_icon_status_discharging '👎'

set -g @batt_color_charge_primary_tier8 'colour142'
set -g @batt_color_charge_primary_tier7 'colour106'
set -g @batt_color_charge_primary_tier6 'colour108'
set -g @batt_color_charge_primary_tier5 'colour72'
set -g @batt_color_charge_primary_tier4 'colour172'
set -g @batt_color_charge_primary_tier3 'colour214'
set -g @batt_color_charge_primary_tier2 'colour124'
set -g @batt_color_charge_primary_tier1 'colour167'

set -g @batt_icon_charge_tier8 ''
set -g @batt_icon_charge_tier7 ''
set -g @batt_icon_charge_tier6 ''
set -g @batt_icon_charge_tier5 ''
set -g @batt_icon_charge_tier4 ''
set -g @batt_icon_charge_tier3 ''
set -g @batt_icon_charge_tier2 ''
set -g @batt_icon_charge_tier1 ''

set -g @batt_icon_status_charged '⚡'
set -g @batt_icon_status_charging '⚡'
set -g @batt_icon_status_discharging '🔻'


set -g status-right-attr 'none'
set -g status-right "#[fg=colour239, bg=colour237, nobold, nounderscore, noitalics]#{cpu_fg_color}#[fg=colour239]#{cpu_bg_color}   #{cpu_icon} #{cpu_percentage}#{online_status}#{network_bandwidth} #[default]#{battery_color_charge_fg}#{battery_color_charge_bg} #{battery_icon_status}#{battery_icon_charge} #{battery_percentage} #[fg=colour239,bg=colour250] %Y-%m-%d %H:%M #[fg=colour248, bg=colour239, nobold, noitalics, nounderscore]"
set -g status-right-length '80'

setw -g window-status-bg default
setw -g window-status-fg colour237

setw -g window-status-current-bg colour214
setw -g window-status-current-fg colour237
setw -g window-status-current-attr bold

setw -g window-status-activity-bg colour237
setw -g window-status-activity-fg colour248

setw -g window-status-attr none

set-window-option -g window-status-current-format "#[fg=colour239, bg=colour248, :nobold, noitalics, nounderscore]#[fg=colour239, bg=colour214] #I #[fg=colour239, bg=colour214, bold] #W #[fg=colour214, bg=colour237, nobold, noitalics, nounderscore]"
set-window-option -g window-status-format "#[fg=colour237,bg=colour239,noitalics]#[fg=colour223,bg=colour239] #I #[fg=colour223, bg=colour239] #W #[fg=colour239, bg=colour237, noitalics]"

setw -g window-status-bell-attr bold
setw -g window-status-bell-fg colour255
setw -g window-status-bell-bg colour1

setw -g mode-bg colour6
setw -g mode-fg colour0

set -g message-bg colour239
set -g message-fg colour223
set -g message-command-bg colour239
set -g message-command-fg colour223

set-option -g display-panes-active-colour colour250 #fg2
set-option -g display-panes-colour colour237 #bg1

# status bar

set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off
set-window-option -g monitor-activity off
set-option -g bell-action none


# reload config
bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded"
bind-key M split-window -h "$EDITOR ~/.tmux.conf"

#set -g status-utf8 on
set -g status-justify left
set -g status-interval 2

# vim pane navigation
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n C-h if-shell "$is_vim" "send-keys C-h"  "select-pane -L"
bind-key -n C-j if-shell "$is_vim" "send-keys C-j"  "select-pane -D"
bind-key -n C-k if-shell "$is_vim" "send-keys C-k"  "select-pane -U"
bind-key -n C-l if-shell "$is_vim" "send-keys C-l"  "select-pane -R"
bind-key -n C-\ if-shell "$is_vim" "send-keys C-\\" "select-pane -l"
bind-key -T copy-mode-vi C-h select-pane -L
bind-key -T copy-mode-vi C-j select-pane -D
bind-key -T copy-mode-vi C-k select-pane -U
bind-key -T copy-mode-vi C-l select-pane -R
bind-key -T copy-mode-vi C-\ select-pane -l


# plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'xamut/tmux-network-bandwidth'
set -g @plugin 'tmux-plugins/tmux-online-status'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'Morantron/tmux-fingers'

set -g @continuum-restore 'on'

run -b '~/.tmux/plugins/tpm/tpm'
